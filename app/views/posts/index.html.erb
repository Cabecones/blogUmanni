<% @posts.each do |post| %>
  <h1><%= post.title %></h1>
  <p><%= post.content %></p>
  <%= link_to 'Editar', edit_post_path(post) %>
  <%= link_to'Apagar', post_path(post), method: :delete, data: { confirm: 'Tem certeza?' } %>
  <%= link_to 'Saiba mais', post_path(post) %>
<% end %>